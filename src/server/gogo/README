
Le serveur gogo est écrit en go (http://golang.org). Il est conçu pour être disponible à l'adresse
	http://canop.org:8000/chrall
	(anciennement sur le port 9090)

Pour le compiler, après avoir installé l'environnement go, lancez la commande suivante dans ce répertoire :
	gomake

Pour lancer le serveur, exécutez ensuite
	gogo
	
Le script restart-gogo.sh compiles, kills and restarts gogo as a background application

Pour le tester, utilisez un navigateur web pour accéder à l'url
	http://localhost:8000/chrall
